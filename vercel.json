{
  "buildCommand": "rm -rf node_modules/.prisma && rm -rf .next && PRISMA_CLIENT_ENGINE_TYPE=binary npx prisma generate && next build",
  "installCommand": "npm install",
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true",
    "PRISMA_CLIENT_ENGINE_TYPE": "binary"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "framework": "nextjs",
  "outputDirectory": ".next"
}
